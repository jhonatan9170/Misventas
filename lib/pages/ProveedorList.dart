import 'package:flutter/material.dart';

class ProveedorList extends StatelessWidget {
  const ProveedorList({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

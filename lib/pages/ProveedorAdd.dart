import 'package:flutter/material.dart';

class ProveedorAdd extends StatelessWidget {
  const ProveedorAdd({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

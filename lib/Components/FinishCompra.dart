import 'package:flutter/material.dart';

class FinishCompra extends StatelessWidget {
  const FinishCompra({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

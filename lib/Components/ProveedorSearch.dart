
import 'package:flutter/material.dart';

class ProveedorSearch extends StatelessWidget {
  const ProveedorSearch({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
